[package]
name = "aurora_image"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Owned image decoders: PNG, JPEG, GIF, BMP, ICO (§5.12)"
publish = false

# Tier 0 — owned (§3.2). Decoders are written here; oracles only in tests.
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
