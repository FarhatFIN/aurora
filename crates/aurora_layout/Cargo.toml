[package]
name = "aurora_layout"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Box building, fragment tree, layout algorithms (§5.9–§5.10)"
publish = false

# Tier 0 — owned (§3.2).
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
