[package]
name = "aurora_ipc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Message types crossing thread boundaries (§4.4)"
publish = false

# Tier 0 — owned (§3.2). Contains no logic.
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
