[package]
name = "aurora_net"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "HTTP/1.1 client: redirects, chunked, decoding, sniffing (§5.2)"
publish = false

# Tier 0 body over Tier 1 TLS (rustls, §3.3) when networking lands in M1.
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
