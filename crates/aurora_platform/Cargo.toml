[package]
name = "aurora_platform"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Window/present glue, clipboard, fonts, DPI (§3.5)"
publish = false

# The only crate with #[cfg(target_os)] and FFI (§3.5, §9.4).
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
