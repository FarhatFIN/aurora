[package]
name = "aurora_url"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "URL parsing, serialization, joining, and origins (§5.1)"
publish = false

# Tier 0 — owned (§3.2).
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
