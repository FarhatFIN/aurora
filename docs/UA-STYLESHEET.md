# UA Stylesheet

The user-agent-origin stylesheet is a product artifact written carefully in
M3 (§7.5) and covered by style tests; every later milestone builds on it. Its
documented surface (default `display` per element, typography block, form
control appearance) is recorded here as it lands.

## Status at M0

Not written yet — M3 owns it (§7.5, WBS §6.2 "UA stylesheet" rows).
