# Glossary

Abbreviations and project terms (§9.1: the established abbreviation set lives
here; everything else is spelled out).

| Term | Meaning |
|---|---|
| bfc | block formatting context (CSS) |
| dpr | device pixel ratio |
| idl | interface definition language (WebIDL, the DOM's interface language) |
| ua | user agent |
| wpt | Web Platform Tests, the cross-browser test suite (§8.6) |
| tdz | temporal dead zone (ECMA-262 `let`/`const` binding state) |
| ADR | architecture decision record (§4.9) |
| WBS | work breakdown structure (Part 6 of the master specification) |
| SOP | same-origin policy (§5.18) |
| CSP | content security policy (§5.18) |
| HSTS | HTTP strict transport security (§5.3) |
