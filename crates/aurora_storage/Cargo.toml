[package]
name = "aurora_storage"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cookies, web storage, HTTP disk cache (§5.17)"
publish = false

# Tier 0 — owned (§3.2). Crash-safe writes: temp-then-rename.
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
