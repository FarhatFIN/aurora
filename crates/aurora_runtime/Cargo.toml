[package]
name = "aurora_runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Event loop, timers, host bindings, console, fetch API (§5.15)"
publish = false

# Tier 0 — owned (§3.2). Fetch-API, event loop, and bindings are written here.
[dependencies]
aurora_net = { path = "../aurora_net" }
aurora_url = { path = "../aurora_url" }
aurora_encoding = { path = "../aurora_encoding" }

[lints]
workspace = true
