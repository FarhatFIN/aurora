[package]
name = "aurora_paint"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Display list, software rasterizer, compositing (§5.16)"
publish = false

# Tier 0 — owned (§3.2). Deterministic output is load-bearing (§3.6).
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
