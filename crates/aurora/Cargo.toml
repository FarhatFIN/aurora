[package]
name = "aurora"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The WebView facade: the engine's only public API (§4.1)"
publish = false

# Tier 0 — owned (§3.2). The facade contains no logic (§4.3): it re-exports
# the engine entry points and hosts the headless `aurora` binary that the
# milestone demos drive (§7.3: `aurora --url …`).

[dependencies]
aurora_runtime = { path = "../aurora_runtime" }

[lints]
workspace = true
