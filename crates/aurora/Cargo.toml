[package]
name = "aurora"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The WebView facade: the engine's only public API (§4.1)"
publish = false

# Tier 0 — owned (§3.2). Contains no logic; versioned semantically from M13.
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
