[package]
name = "aurora_js"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "JavaScript lexer, parser, interpreter, GC (§5.13–§5.14)"
publish = false

# Tier 0 — owned (§3.2). Must never know about the DOM (§4.3 bridge rule).
# Zero dependencies at M0; §3.3 Tier 1 additions require the milestone that
# needs them plus a one-line justification here.

[lints]
workspace = true
